# Overview

This repository is organized around one central idea:

Modern LLM architectures are best understood as configurable compute graphs rather than isolated branded models.

Instead of treating each model family as a separate object, this repository reduces architectures into reusable primitives:

1. representation
2. interaction
3. selection
4. transformation
5. composition
6. projection
